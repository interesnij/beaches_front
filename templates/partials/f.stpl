<!-- End::app-content -->
        <% include!("footer.stpl"); %>

    </div>
    <script>
        function getCookie(name) {
            const cookies = document.cookie.split(';');
            for (let i = 0; i < cookies.length; i++) {
                let c = cookies[i].trim().split('=');
                if (c[0] === name) {
                    return c[1];
                }
            }
            return "";
        }

        function get_background() {
            background = getCookie("background");
            if (background == "dark") {
                document.setAttribute("data-theme-mode", "dark");
                document.setAttribute("data-header-styles", "dark");
                document.setAttribute("data-menu-styles", "dark");
            }
            else {
                document.setAttribute("data-theme-mode", "light");
                document.setAttribute("data-header-styles", "light");
                document.setAttribute("data-menu-styles", "light");
            }
        }
        get_background();
    </script>
    <% include!("commonjs.stpl"); %>

    <!-- Custom JS -->
    <script src="/assets/js/custom.js"></script>
    <script src="/assets/js/work.js"></script>
</body>
</html>